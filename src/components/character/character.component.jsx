import React from 'react';
import schemaPropType from '../../schema/schemaPropType';

const Character = () => (
  <div className="character">CHARACTER</div>
);

Character.propTypes = {
};

export default Character;
